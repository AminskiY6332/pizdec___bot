# handlers/admin/commands.py
"""
Административные команды

Перенесено из handlers/commands.py:
- addcook() - добавление печенек пользователю
- delcook() - удаление печенек у пользователя  
- addnew() - добавление подписки пользователю
- delnew() - удаление подписки у пользователя
- user_id_info() - информация о пользователе
- dev_test_tariff() - тестовый тариф для разработчика
- debug_avatars() - отладка аватаров
"""

import logging
import asyncio
import os
import pytz
import aiosqlite
from aiogram import Bot
from datetime import datetime
from aiogram.types import Message, InlineKeyboardMarkup, InlineKeyboardButton, FSInputFile
from aiogram.fsm.context import FSMContext
from aiogram.enums import ParseMode
from config import ADMIN_IDS, REPLICATE_USERNAME_OR_ORG_NAME
from database import (
    add_user_without_subscription,
    check_user_resources,
    update_user_balance,
    is_old_user,
    get_user_actions_stats,
    is_user_blocked,
    get_user_trainedmodels,
    get_active_trainedmodel,
    check_database_user,
    get_user_payments,
    update_user_credits,
    get_user_info,
    get_user_by_username,
    get_user_generation_stats
)
from keyboards import create_main_menu_keyboard, create_subscription_keyboard, create_user_profile_keyboard, create_payment_only_keyboard
from generation import reset_generation_context, check_training_status
from handlers.utils import safe_escape_markdown as escape_md, get_tariff_text, send_message_with_fallback
from ..user.onboarding import send_onboarding_message, schedule_welcome_message, schedule_daily_reminders
from bot_counter import bot_counter

from logger import get_logger
logger = get_logger('main')

async def dev_test_tariff(message: Message, state: FSMContext) -> None:
    """Показывает тестовый тариф только главному разработчику."""
    user_id = message.from_user.id

    # Проверяем, что это главный разработчик
    if user_id != 5667999089:
        await message.answer("❌ Команда доступна только главному разработчику.")
        return

    from config import TARIFFS
    test_tariff = TARIFFS.get("тест")

    if not test_tariff:
        await message.answer("❌ Тестовый тариф не найден в конфигурации.")
        return

    text = escape_md(f"""🧪 ТЕСТОВЫЙ ТАРИФ ДЛЯ ПРОВЕРКИ ПЛАТЕЖЕЙ

💎 {test_tariff['display']}
📝 Описание: {test_tariff['description']}
💰 Сумма: {test_tariff['amount']:.2f}₽
🍪 Печеньки: {test_tariff['photos']}

🔗 Это специальный тариф для проверки работы платежной системы.""", version=2)

    keyboard = InlineKeyboardMarkup(inline_keyboard=[
        [InlineKeyboardButton(text=test_tariff["display"], callback_data=test_tariff["callback"])],
        [InlineKeyboardButton(text="🏠 В меню", callback_data="back_to_menu")]
    ])

    await message.answer(
        text,
        reply_markup=keyboard,
        parse_mode=ParseMode.MARKDOWN_V2
    )

    logger.info(f"Тестовый тариф показан главному разработчику user_id={user_id}")
    await state.update_data(user_id=user_id)

async def debug_avatars(message: Message, state: FSMContext) -> None:
    """Отладочная команда для проверки аватаров (только для админов)."""
    user_id = message.from_user.id

    if user_id not in ADMIN_IDS:
        await message.answer(
            escape_md("❌ У вас нет прав для этой команды.", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )
        return

    if await is_user_blocked(user_id):
        logger.info(f"Заблокированный пользователь user_id={user_id} пытался выполнить действие")
        return

    bot = message.bot

    try:
        models = await get_user_trainedmodels(user_id)

        if not models:
            await message.answer(
                escape_md("❌ У вас нет обученных аватаров.", version=2),
                parse_mode=ParseMode.MARKDOWN_V2
            )
            return

        active_model = await get_active_trainedmodel(user_id)
        
        text = escape_md(f"🔍 ОТЛАДКА АВАТАРОВ\n\n", version=2)
        
        for model in models:
            status = "✅ АКТИВЕН" if model['id'] == active_model['id'] else "⏸️ НЕАКТИВЕН"
            text += escape_md(f"🎭 {model['name']}\n", version=2)
            text += escape_md(f"📊 Статус: {status}\n", version=2)
            text += escape_md(f"🆔 ID: {model['id']}\n", version=2)
            text += escape_md(f"📅 Создан: {model['created_at']}\n", version=2)
            text += escape_md(f"🔄 Обновлен: {model['updated_at']}\n\n", version=2)

        await message.answer(
            text,
            parse_mode=ParseMode.MARKDOWN_V2
        )

    except Exception as e:
        logger.error(f"Ошибка в debug_avatars: {e}", exc_info=True)
        await message.answer(
            escape_md("❌ Ошибка при получении информации об аватарах.", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )

async def addcook(message: Message, state: FSMContext) -> None:
    """Добавляет печеньки пользователю (только для админов)."""
    user_id = message.from_user.id

    if user_id not in ADMIN_IDS:
        await message.answer(
            escape_md("❌ У вас нет прав для этой команды.", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )
        return

    if await is_user_blocked(user_id):
        logger.info(f"Заблокированный пользователь user_id={user_id} пытался выполнить действие")
        return

    try:
        # Парсим команду: /addcook user_id количество
        parts = message.text.split()
        if len(parts) != 3:
            await message.answer(
                escape_md("❌ Неверный формат команды.\nИспользуйте: /addcook user_id количество\nПример: /addcook 123456 30", version=2),
                parse_mode=ParseMode.MARKDOWN_V2
            )
            return

        target_user_id = int(parts[1])
        amount = int(parts[2])

        if amount <= 0:
            await message.answer(
                escape_md("❌ Количество печенек должно быть положительным.", version=2),
                parse_mode=ParseMode.MARKDOWN_V2
            )
            return

        # Проверяем существование пользователя
        user_info = await get_user_info(target_user_id)
        if not user_info:
            await message.answer(
                escape_md(f"❌ Пользователь с ID {target_user_id} не найден.", version=2),
                parse_mode=ParseMode.MARKDOWN_V2
            )
            return

        # Показываем подтверждение
        text = escape_md(f"🔧 ПОДТВЕРЖДЕНИЕ ДОБАВЛЕНИЯ ПЕЧЕНЕК\n\n", version=2)
        text += escape_md(f"👤 Пользователь: {user_info['username'] or 'Без имени'}\n", version=2)
        text += escape_md(f"🆔 ID: {target_user_id}\n", version=2)
        text += escape_md(f"🍪 Текущий баланс: {user_info['generations_left']}\n", version=2)
        text += escape_md(f"➕ Добавить: {amount}\n", version=2)
        text += escape_md(f"📊 Новый баланс: {user_info['generations_left'] + amount}\n\n", version=2)
        text += escape_md(f"Подтвердите действие:", version=2)

        keyboard = InlineKeyboardMarkup(inline_keyboard=[
            [InlineKeyboardButton(text="✅ Подтвердить", callback_data=f"confirm_addcook_{target_user_id}_{amount}")],
            [InlineKeyboardButton(text="❌ Отмена", callback_data="cancel_admin_operation")]
        ])

        await message.answer(
            text,
            reply_markup=keyboard,
            parse_mode=ParseMode.MARKDOWN_V2
        )

    except ValueError:
        await message.answer(
            escape_md("❌ Неверный формат числа.", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )
    except Exception as e:
        logger.error(f"Ошибка в addcook: {e}", exc_info=True)
        await message.answer(
            escape_md("❌ Ошибка при обработке команды.", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )

async def delcook(message: Message, state: FSMContext) -> None:
    """Удаляет печеньки у пользователя (только для админов)."""
    user_id = message.from_user.id

    if user_id not in ADMIN_IDS:
        await message.answer(
            escape_md("❌ У вас нет прав для этой команды.", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )
        return

    if await is_user_blocked(user_id):
        logger.info(f"Заблокированный пользователь user_id={user_id} пытался выполнить действие")
        return

    try:
        # Парсим команду: /delcook user_id количество
        parts = message.text.split()
        if len(parts) != 3:
            await message.answer(
                escape_md("❌ Неверный формат команды.\nИспользуйте: /delcook user_id количество\nПример: /delcook 123456 10", version=2),
                parse_mode=ParseMode.MARKDOWN_V2
            )
            return

        target_user_id = int(parts[1])
        amount = int(parts[2])

        if amount <= 0:
            await message.answer(
                escape_md("❌ Количество печенек должно быть положительным.", version=2),
                parse_mode=ParseMode.MARKDOWN_V2
            )
            return

        # Проверяем существование пользователя
        user_info = await get_user_info(target_user_id)
        if not user_info:
            await message.answer(
                escape_md(f"❌ Пользователь с ID {target_user_id} не найден.", version=2),
                parse_mode=ParseMode.MARKDOWN_V2
            )
            return

        # Показываем подтверждение
        text = escape_md(f"🔧 ПОДТВЕРЖДЕНИЕ УДАЛЕНИЯ ПЕЧЕНЕК\n\n", version=2)
        text += escape_md(f"👤 Пользователь: {user_info['username'] or 'Без имени'}\n", version=2)
        text += escape_md(f"🆔 ID: {target_user_id}\n", version=2)
        text += escape_md(f"🍪 Текущий баланс: {user_info['generations_left']}\n", version=2)
        text += escape_md(f"➖ Удалить: {amount}\n", version=2)
        text += escape_md(f"📊 Новый баланс: {max(0, user_info['generations_left'] - amount)}\n\n", version=2)
        text += escape_md(f"Подтвердите действие:", version=2)

        keyboard = InlineKeyboardMarkup(inline_keyboard=[
            [InlineKeyboardButton(text="✅ Подтвердить", callback_data=f"confirm_delcook_{target_user_id}_{amount}")],
            [InlineKeyboardButton(text="❌ Отмена", callback_data="cancel_admin_operation")]
        ])

        await message.answer(
            text,
            reply_markup=keyboard,
            parse_mode=ParseMode.MARKDOWN_V2
        )

    except ValueError:
        await message.answer(
            escape_md("❌ Неверный формат числа.", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )
    except Exception as e:
        logger.error(f"Ошибка в delcook: {e}", exc_info=True)
        await message.answer(
            escape_md("❌ Ошибка при обработке команды.", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )

async def addnew(message: Message, state: FSMContext) -> None:
    """Добавляет аватары (печеньки) пользователю (только для админов)."""
    user_id = message.from_user.id

    if user_id not in ADMIN_IDS:
        await message.answer(
            escape_md("❌ У вас нет прав для этой команды.", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )
        return

    if await is_user_blocked(user_id):
        logger.info(f"Заблокированный пользователь user_id={user_id} пытался выполнить действие")
        return

    try:
        # Парсим команду: /addnew user_id количество
        parts = message.text.split()
        if len(parts) != 3:
            await message.answer(
                escape_md("❌ Неверный формат команды.\nИспользуйте: /addnew user_id количество\nПример: /addnew 123456 30", version=2),
                parse_mode=ParseMode.MARKDOWN_V2
            )
            return

        target_user_id = int(parts[1])
        amount = int(parts[2])

        if amount <= 0:
            await message.answer(
                escape_md("❌ Количество аватаров должно быть положительным.", version=2),
                parse_mode=ParseMode.MARKDOWN_V2
            )
            return

        # Проверяем существование пользователя
        user_info = await get_user_info(target_user_id)
        if not user_info:
            await message.answer(
                escape_md(f"❌ Пользователь с ID {target_user_id} не найден.", version=2),
                parse_mode=ParseMode.MARKDOWN_V2
            )
            return

        # Показываем подтверждение
        text = escape_md(f"🔧 ПОДТВЕРЖДЕНИЕ ДОБАВЛЕНИЯ АВАТАРОВ\n\n", version=2)
        text += escape_md(f"👤 Пользователь: {user_info['username'] or 'Без имени'}\n", version=2)
        text += escape_md(f"🆔 ID: {target_user_id}\n", version=2)
        text += escape_md(f"🎭 Текущий баланс аватаров: {user_info['generations_left']}\n", version=2)
        text += escape_md(f"➕ Добавить: {amount}\n", version=2)
        text += escape_md(f"📊 Новый баланс: {user_info['generations_left'] + amount}\n\n", version=2)
        text += escape_md(f"Подтвердите действие:", version=2)

        keyboard = InlineKeyboardMarkup(inline_keyboard=[
            [InlineKeyboardButton(text="✅ Подтвердить", callback_data=f"confirm_addnew_{target_user_id}_{amount}")],
            [InlineKeyboardButton(text="❌ Отмена", callback_data="cancel_admin_operation")]
        ])

        await message.answer(
            text,
            reply_markup=keyboard,
            parse_mode=ParseMode.MARKDOWN_V2
        )

    except ValueError:
        await message.answer(
            escape_md("❌ Неверный формат числа.", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )
    except Exception as e:
        logger.error(f"Ошибка в addnew: {e}", exc_info=True)
        await message.answer(
            escape_md("❌ Ошибка при обработке команды.", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )

async def delnew(message: Message, state: FSMContext) -> None:
    """Удаляет аватары (печеньки) у пользователя (только для админов)."""
    user_id = message.from_user.id

    if user_id not in ADMIN_IDS:
        await message.answer(
            escape_md("❌ У вас нет прав для этой команды.", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )
        return

    if await is_user_blocked(user_id):
        logger.info(f"Заблокированный пользователь user_id={user_id} пытался выполнить действие")
        return

    try:
        # Парсим команду: /delnew user_id количество
        parts = message.text.split()
        if len(parts) != 3:
            await message.answer(
                escape_md("❌ Неверный формат команды.\nИспользуйте: /delnew user_id количество\nПример: /delnew 123456 10", version=2),
                parse_mode=ParseMode.MARKDOWN_V2
            )
            return

        target_user_id = int(parts[1])
        amount = int(parts[2])

        if amount <= 0:
            await message.answer(
                escape_md("❌ Количество аватаров должно быть положительным.", version=2),
                parse_mode=ParseMode.MARKDOWN_V2
            )
            return

        # Проверяем существование пользователя
        user_info = await get_user_info(target_user_id)
        if not user_info:
            await message.answer(
                escape_md(f"❌ Пользователь с ID {target_user_id} не найден.", version=2),
                parse_mode=ParseMode.MARKDOWN_V2
            )
            return

        # Показываем подтверждение
        text = escape_md(f"🔧 ПОДТВЕРЖДЕНИЕ УДАЛЕНИЯ АВАТАРОВ\n\n", version=2)
        text += escape_md(f"👤 Пользователь: {user_info['username'] or 'Без имени'}\n", version=2)
        text += escape_md(f"🆔 ID: {target_user_id}\n", version=2)
        text += escape_md(f"🎭 Текущий баланс аватаров: {user_info['generations_left']}\n", version=2)
        text += escape_md(f"➖ Удалить: {amount}\n", version=2)
        text += escape_md(f"📊 Новый баланс: {max(0, user_info['generations_left'] - amount)}\n\n", version=2)
        text += escape_md(f"Подтвердите действие:", version=2)

        keyboard = InlineKeyboardMarkup(inline_keyboard=[
            [InlineKeyboardButton(text="✅ Подтвердить", callback_data=f"confirm_delnew_{target_user_id}_{amount}")],
            [InlineKeyboardButton(text="❌ Отмена", callback_data="cancel_admin_operation")]
        ])

        await message.answer(
            text,
            reply_markup=keyboard,
            parse_mode=ParseMode.MARKDOWN_V2
        )

    except ValueError:
        await message.answer(
            escape_md("❌ Неверный формат числа.", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )
    except Exception as e:
        logger.error(f"Ошибка в delnew: {e}", exc_info=True)
        await message.answer(
            escape_md("❌ Ошибка при обработке команды.", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )

async def user_id_info(message: Message, state: FSMContext) -> None:
    """Показывает информацию о пользователе (только для админов)."""
    user_id = message.from_user.id

    if user_id not in ADMIN_IDS:
        await message.answer(
            escape_md("❌ У вас нет прав для этой команды.", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )
        return

    if await is_user_blocked(user_id):
        logger.info(f"Заблокированный пользователь user_id={user_id} пытался выполнить действие")
        return

    try:
        # Парсим команду: /id user_id
        parts = message.text.split()
        if len(parts) != 2:
            await message.answer(
                escape_md("❌ Неверный формат команды.\nИспользуйте: /id user_id\nПример: /id 123456", version=2),
                parse_mode=ParseMode.MARKDOWN_V2
            )
            return

        target_user_id = int(parts[1])

        # Получаем информацию о пользователе
        user_info = await get_user_info(target_user_id)
        if not user_info:
            await message.answer(
                escape_md(f"❌ Пользователь с ID {target_user_id} не найден.", version=2),
                parse_mode=ParseMode.MARKDOWN_V2
            )
            return

        # Получаем статистику
        stats = await get_user_actions_stats(target_user_id)
        payments = await get_user_payments(target_user_id)
        generation_stats = await get_user_generation_stats(target_user_id)

        text = escape_md(f"👤 ИНФОРМАЦИЯ О ПОЛЬЗОВАТЕЛЕ\n\n", version=2)
        text += escape_md(f"🆔 ID: {target_user_id}\n", version=2)
        text += escape_md(f"👤 Имя: {user_info['username'] or 'Без имени'}\n", version=2)
        text += escape_md(f"📧 Email: {user_info['email'] or 'Не указан'}\n", version=2)
        text += escape_md(f"🍪 Печеньки: {user_info['credits']}\n", version=2)
        text += escape_md(f"📅 Подписка до: {user_info['subscription_end'] or 'Нет'}\n", version=2)
        text += escape_md(f"📊 Действий: {stats.get('total_actions', 0)}\n", version=2)
        text += escape_md(f"💰 Платежей: {len(payments)}\n", version=2)
        text += escape_md(f"🎭 Генераций: {generation_stats.get('total_generations', 0)}\n", version=2)
        text += escape_md(f"📅 Регистрация: {user_info['created_at']}\n", version=2)

        await message.answer(
            text,
            parse_mode=ParseMode.MARKDOWN_V2
        )

    except ValueError:
        await message.answer(
            escape_md("❌ Неверный формат числа.", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )
    except Exception as e:
        logger.error(f"Ошибка в user_id_info: {e}", exc_info=True)
        await message.answer(
            escape_md("❌ Ошибка при получении информации о пользователе.", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )

# Обработчики callback'ов для админских команд
from aiogram.types import CallbackQuery
from aiogram import F

async def confirm_addcook_callback(query: CallbackQuery, state: FSMContext) -> None:
    """Подтверждение добавления печенек."""
    user_id = query.from_user.id
    
    if user_id not in ADMIN_IDS:
        await query.answer("❌ У вас нет прав.", show_alert=True)
        return
    
    try:
        # Парсим callback_data: confirm_addcook_{target_user_id}_{amount}
        parts = query.data.split('_')
        target_user_id = int(parts[2])
        amount = int(parts[3])
        
        # Получаем информацию о пользователе
        user_info = await get_user_info(target_user_id)
        if not user_info:
            await query.answer("❌ Пользователь не найден.", show_alert=True)
            return
        
        # Обновляем баланс
        new_balance = user_info['generations_left'] + amount
        await update_user_balance(target_user_id, amount, 0)  # добавляем только печеньки, аватары не трогаем
        
        # Отправляем подтверждение
        await query.message.edit_text(
            escape_md(f"✅ Печеньки успешно добавлены!\n\n👤 Пользователь: {user_info['username'] or 'Без имени'}\n🆔 ID: {target_user_id}\n🍪 Новый баланс: {new_balance}", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )
        await query.answer("✅ Операция выполнена!", show_alert=True)
        
    except Exception as e:
        logger.error(f"Ошибка в confirm_addcook_callback: {e}", exc_info=True)
        await query.answer("❌ Ошибка при выполнении операции.", show_alert=True)

async def confirm_delcook_callback(query: CallbackQuery, state: FSMContext) -> None:
    """Подтверждение удаления печенек."""
    user_id = query.from_user.id
    
    if user_id not in ADMIN_IDS:
        await query.answer("❌ У вас нет прав.", show_alert=True)
        return
    
    try:
        # Парсим callback_data: confirm_delcook_{target_user_id}_{amount}
        parts = query.data.split('_')
        target_user_id = int(parts[2])
        amount = int(parts[3])
        
        # Получаем информацию о пользователе
        user_info = await get_user_info(target_user_id)
        if not user_info:
            await query.answer("❌ Пользователь не найден.", show_alert=True)
            return
        
        # Обновляем баланс
        new_balance = max(0, user_info['generations_left'] - amount)
        await update_user_balance(target_user_id, amount, 0, 'subtract')  # удаляем только печеньки, аватары не трогаем
        
        # Отправляем подтверждение
        await query.message.edit_text(
            escape_md(f"✅ Печеньки успешно удалены!\n\n👤 Пользователь: {user_info['username'] or 'Без имени'}\n🆔 ID: {target_user_id}\n🍪 Новый баланс: {new_balance}", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )
        await query.answer("✅ Операция выполнена!", show_alert=True)
        
    except Exception as e:
        logger.error(f"Ошибка в confirm_delcook_callback: {e}", exc_info=True)
        await query.answer("❌ Ошибка при выполнении операции.", show_alert=True)

async def confirm_addnew_callback(query: CallbackQuery, state: FSMContext) -> None:
    """Подтверждение добавления аватаров."""
    user_id = query.from_user.id
    
    if user_id not in ADMIN_IDS:
        await query.answer("❌ У вас нет прав.", show_alert=True)
        return
    
    try:
        # Парсим callback_data: confirm_addnew_{target_user_id}_{amount}
        parts = query.data.split('_')
        target_user_id = int(parts[2])
        amount = int(parts[3])
        
        # Получаем информацию о пользователе
        user_info = await get_user_info(target_user_id)
        if not user_info:
            await query.answer("❌ Пользователь не найден.", show_alert=True)
            return
        
        # Обновляем баланс аватаров
        new_balance = user_info['generations_left'] + amount
        await update_user_balance(target_user_id, 0, amount)  # добавляем только аватары, печеньки не трогаем
        
        # Отправляем подтверждение
        await query.message.edit_text(
            escape_md(f"✅ Аватары успешно добавлены!\n\n👤 Пользователь: {user_info['username'] or 'Без имени'}\n🆔 ID: {target_user_id}\n🎭 Новый баланс: {new_balance}", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )
        await query.answer("✅ Операция выполнена!", show_alert=True)
        
    except Exception as e:
        logger.error(f"Ошибка в confirm_addnew_callback: {e}", exc_info=True)
        await query.answer("❌ Ошибка при выполнении операции.", show_alert=True)

async def confirm_delnew_callback(query: CallbackQuery, state: FSMContext) -> None:
    """Подтверждение удаления аватаров."""
    user_id = query.from_user.id
    
    if user_id not in ADMIN_IDS:
        await query.answer("❌ У вас нет прав.", show_alert=True)
        return
    
    try:
        # Парсим callback_data: confirm_delnew_{target_user_id}_{amount}
        parts = query.data.split('_')
        target_user_id = int(parts[2])
        amount = int(parts[3])
        
        # Получаем информацию о пользователе
        user_info = await get_user_info(target_user_id)
        if not user_info:
            await query.answer("❌ Пользователь не найден.", show_alert=True)
            return
        
        # Обновляем баланс аватаров
        new_balance = max(0, user_info['generations_left'] - amount)
        await update_user_balance(target_user_id, 0, amount, 'subtract')  # удаляем только аватары
        
        # Отправляем подтверждение
        await query.message.edit_text(
            escape_md(f"✅ Аватары успешно удалены!\n\n👤 Пользователь: {user_info['username'] or 'Без имени'}\n🆔 ID: {target_user_id}\n🎭 Новый баланс: {new_balance}", version=2),
            parse_mode=ParseMode.MARKDOWN_V2
        )
        await query.answer("✅ Операция выполнена!", show_alert=True)
        
    except Exception as e:
        logger.error(f"Ошибка в confirm_delnew_callback: {e}", exc_info=True)
        await query.answer("❌ Ошибка при выполнении операции.", show_alert=True)

async def cancel_admin_operation_callback(query: CallbackQuery, state: FSMContext) -> None:
    """Отмена админской операции."""
    user_id = query.from_user.id
    
    if user_id not in ADMIN_IDS:
        await query.answer("❌ У вас нет прав.", show_alert=True)
        return
    
    await query.message.edit_text(
        escape_md("❌ Операция отменена.", version=2),
        parse_mode=ParseMode.MARKDOWN_V2
    )
    await query.answer("❌ Операция отменена", show_alert=True)
